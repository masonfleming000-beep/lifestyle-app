{
	"name": "lifestyle-app",
	"type": "module",
	"version": "0.0.1",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && wrangler pages dev",
		"astro": "astro",
		"generate-types": "wrangler types",
		"deploy": "astro build && wrangler pages deploy dist",
		"preview:mobile": "astro build && wrangler pages dev dist --ip 0.0.0.0",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^13.1.2",
		"@supabase/ssr": "^0.9.0",
		"@supabase/supabase-js": "^2.99.2",
		"astro": "^6.0.5"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260317.1",
		"@types/node": "^25.5.0",
		"wrangler": "^4.75.0"
	}
}
