---
/**
 * Generic shared card shell.
 */
interface Props {
  as?: "article" | "div" | "section";
  class?: string;
  id?: string;
}

const {
  as = "article",
  class: className = "",
  id = "",
} = Astro.props;

const Tag = as;
---

<Tag id={id || undefined} class:list={["card", "surface-card", className]}>
  <slot />
</Tag>

<style>
  .surface-card {
    display: grid;
    gap: 1rem;
  }
</style>
