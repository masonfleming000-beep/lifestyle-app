---
import type { FieldConfig } from '../../lib/ui/types';
interface Props { field: FieldConfig; }
const { field } = Astro.props;
---
<label class="form-stack">
  <span>{field.label}</span>
  <input id={field.id} name={field.name} type="number" value={String(field.value ?? '')} min={field.min} max={field.max} step={field.step} placeholder={field.placeholder} />
  {field.helpText && <small>{field.helpText}</small>}
</label>
