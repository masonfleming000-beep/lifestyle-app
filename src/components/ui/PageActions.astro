---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class:list={["page-actions", className]}>
  <slot />
</div>

<style>
  .page-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 0.9rem;
  }
</style>
